import * as ping from "./ping";
import * as jokes from "./jokes";
import * as translate from "./translate";
import * as info from "./info";

export const commands = {
    ping, jokes, translate, info
};